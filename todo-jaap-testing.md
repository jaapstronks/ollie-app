# Run the test

maestro test .maestro/scenarios/first-time-user-journey.yaml

# With debug output (slower, shows what's happening)

maestro test .maestro/scenarios/first-time-user-journey.yaml --debug-output

# Run the test

maestro test .maestro/scenarios/first-time-user-journey.yaml

# With debug output (slower, shows what's happening)

maestro test .maestro/scenarios/first-time-user-journey.yaml --debug-output
